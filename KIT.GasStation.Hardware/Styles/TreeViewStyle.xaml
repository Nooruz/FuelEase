<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm">



    <Style x:Key="HardWareTreeViewItem" TargetType="{x:Type TreeViewItem}">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ContextMenu x:Key="ControllerContextMenu">
        <MenuItem Command="{Binding ControllerCreateCommand}" Header="Добавить ТРК" />
    </ContextMenu>

    <ContextMenu x:Key="CashRegisterContextMenu">
        <MenuItem Command="{Binding CashRegisterCreateCommand}" Header="Добавить ККМ" />
    </ContextMenu>

    <ContextMenu x:Key="KITContextMenu">
        <MenuItem Command="{Binding KITCreateCommand}" Header="Добавить" />
    </ContextMenu>

    <Style x:Key="HardWareTreeView" TargetType="{x:Type TreeView}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource HardWareTreeViewItem}" />
    </Style>

</ResourceDictionary>