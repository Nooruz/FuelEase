<local:BaseView
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:KIT.GasStation.Views.Base"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="500"
    x:Class="KIT.GasStation.Views.ControllerListView">

    <dxdo:DockLayoutManager>
        <dxdo:LayoutGroup x:Name="LayoutRoot">
            <dxdo:DocumentGroup MDIStyle="MDI"
                                Orientation="Vertical"
                                ItemsSource="{Binding FuelDispenserViewModels}">
                <dxmvvm:Interaction.Behaviors>
                    <dxmvvm:EventToCommand Event="Loaded"
                                           Command="{Binding DocumentGroupLoadedCommand}"
                                           PassEventArgsToCommand="True"/>
                </dxmvvm:Interaction.Behaviors>
                <dxdo:DocumentGroup.ItemStyle>
                    <Style TargetType="{x:Type dxdo:DocumentPanel}" 
                           BasedOn="{StaticResource FuelTransferColumn}">
                        <Setter Property="dxmvvm:Interaction.BehaviorsTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <ItemsControl>
                                        <dxmvvm:EventToCommand Event="Loaded"
                                                               Command="{Binding Path=DataContext.DocumentPanelLoadedCommand, RelativeSource={RelativeSource AncestorType={x:Type local:BaseView}}}" 
                                                               PassEventArgsToCommand="True"/>
                                    </ItemsControl>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </dxdo:DocumentGroup.ItemStyle>
            </dxdo:DocumentGroup>
        </dxdo:LayoutGroup>
    </dxdo:DockLayoutManager>

</local:BaseView>
