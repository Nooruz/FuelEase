<base:BaseView     x:Class="KIT.GasStation.Views.GlobalReportView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
                   xmlns:dxr="http://schemas.devexpress.com/winfx/2008/xaml/ribbon"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                   xmlns:dxa="http://schemas.devexpress.com/winfx/2008/xaml/accordion"
                   xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
                   xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
                   xmlns:dxwuin="http://schemas.devexpress.com/winfx/2008/xaml/windowsui/navigation"
                   xmlns:splashScreen="clr-namespace:KIT.GasStation.SplashScreen.ThemedSplashScreen"
                   xmlns:base="clr-namespace:KIT.GasStation.Views.Base"
                   mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <base:BaseView.Resources>
        <dxmvvm:ObjectToObjectConverter x:Key="expandedToWidthConverter" DefaultTarget="183">
            <dxmvvm:MapItem Source="False" Target="Auto"/>
        </dxmvvm:ObjectToObjectConverter>
    </base:BaseView.Resources>

    <dx:LoadingDecorator>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <dxr:RibbonControl x:Name="ribbon"
                       RibbonStyle="Office2010"
                       AllowCustomization="False"/>

            <dxdo:DockLayoutManager Grid.Row="1"
                        Margin="6">

                <dxdo:LayoutGroup Caption="LayoutRoot">

                    <dxdo:LayoutPanel Caption="WPF Products"
                          ItemWidth="{Binding IsExpanded, Converter={StaticResource expandedToWidthConverter}, ElementName=accordion}"
                          AllowClose="False" 
                          ShowCaption="False" 
                          x:Name="layoutPanel" 
                          AllowSizing="False">

                        <dxa:AccordionControl x:Name="accordion"
                                  SelectedItem="{Binding SelectedModuleViewModel}"
                                  ItemsSource="{Binding ModuleViewModels}"
                                  HeaderContent="Навигация"
                                  SummaryItemHeader="Навигация"
                                  IsCollapseButtonVisible="True"
                                  RootItemDisplayMode="Item"
                                  ShowPopupOnHover="True">
                            <dxa:AccordionControl.ItemContainerStyle>
                                <Style TargetType="{x:Type dxa:AccordionItem}">
                                    <Setter Property="Header" Value="{Binding Title}" />
                                    <Setter Property="Glyph" Value="{Binding Icon}" />
                                    <Setter Property="GlyphPosition" Value="Top"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="ExpandButtonPosition" Value="None"/>
                                </Style>
                            </dxa:AccordionControl.ItemContainerStyle>
                            <dxmvvm:Interaction.Triggers>
                                <dxmvvm:EventToCommand EventName="Loaded" 
                                               Command="{Binding OnModulesLoadedCommand}"/>
                                <dxmvvm:EventToCommand EventName="SelectedItemChanged"
                                               Command="{Binding OnSelectedModuleViewModelChangedCommand}"/>
                            </dxmvvm:Interaction.Triggers>
                        </dxa:AccordionControl>

                    </dxdo:LayoutPanel>

                    <dxdo:LayoutPanel AllowClose="False" 
                              AllowFloat="False" 
                              AllowHide="False" 
                              ShowCaption="False" 
                              ShowBorder="False" 
                              ShowCloseButton="False">

                        <dxwui:NavigationFrame x:Name="documentFrame"
                                       Navigating="OnDocumentFrameNavigating" 
                                       Navigated="OnDocumentFrameNavigated">
                            <dxwui:NavigationFrame.Resources>
                                <Style TargetType="dxwui:PageAdornerControl">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="dxwui:PageAdornerControl">
                                                <ContentPresenter Content="{TemplateBinding Content}" />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </dxwui:NavigationFrame.Resources>
                            <dxmvvm:Interaction.Behaviors>
                                <dxwuin:FrameNavigationService ShowSplashScreen="{Binding CanShowSplashScreen}" 
                                                       Frame="{Binding ElementName=documentFrame}" >
                                    <dxwuin:FrameNavigationService.SplashScreenService>
                                        <dx:SplashScreenManagerService SplashScreenType="{x:Type splashScreen:ProgressWindow}"
                                                               />
                                    </dxwuin:FrameNavigationService.SplashScreenService>
                                </dxwuin:FrameNavigationService>
                            </dxmvvm:Interaction.Behaviors>
                        </dxwui:NavigationFrame>

                    </dxdo:LayoutPanel>

                </dxdo:LayoutGroup>
            </dxdo:DockLayoutManager>

            <dxr:RibbonStatusBarControl x:Name="statusBar" Grid.Row="2">
                <dxr:RibbonStatusBarControl.RightItems>
                    <dxb:BarCheckItem x:Name="layoutNormal" 
                              IsChecked="{Binding IsExpanded, ElementName=accordion, Mode=TwoWay}" 
                              GlyphSize="Small"
                              Glyph="pack://application:,,,/КИТ-АЗС;component/Images/NormalView.svg"/>
                    <dxb:BarCheckItem x:Name="layoutReading" 
                              IsChecked="{Binding IsExpanded, ElementName=accordion, Mode=TwoWay, Converter={dxmvvm:BooleanNegationConverter}}"  
                              GlyphSize="Small"
                              Glyph="pack://application:,,,/КИТ-АЗС;component/Images/ReadingView.svg"/>
                </dxr:RibbonStatusBarControl.RightItems>
            </dxr:RibbonStatusBarControl>

        </Grid>

    </dx:LoadingDecorator>

</base:BaseView>
