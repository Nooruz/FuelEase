<local:BaseView
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    x:Class="KIT.GasStation.Views.Details.FuelDetailView"
    xmlns:local="clr-namespace:KIT.GasStation.Views.Base"
    Width="400">

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:EventToCommand Event="Loaded"
                               Command="{Binding UserControlLoadedCommand}"/>
    </dxmvvm:Interaction.Behaviors>

    <dxlc:LayoutControl Orientation="Vertical">

        <dxlc:LayoutItem Label="Наименование">
            <dxe:TextEdit EditValue="{Binding Fuel.Name, UpdateSourceTrigger=PropertyChanged}"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="Цена">
            <dxe:TextEdit MaskUseAsDisplayFormat="True" MaskType="Numeric" Mask="n"
                          EditValue="{Binding Fuel.Price, UpdateSourceTrigger=PropertyChanged}"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="Единица измерения">
            <dxe:ComboBoxEdit ItemsSource="{Binding UnitOfMeasurements}"
                              DisplayMember="Name"
                              ValueMember="Id" 
                              ShowError="False" 
                              ShowErrorToolTip="False"
                              EditValue="{Binding Fuel.UnitOfMeasurementId}"/>
        </dxlc:LayoutItem>

        <dxlc:LayoutItem Label="Свет">
            <dxe:PopupColorEdit Color="{Binding Color, Mode=TwoWay}"/>
        </dxlc:LayoutItem>

        <dxlc:LayoutItem Label="ТНВЭД">
            <dxe:TextEdit EditValue="{Binding Fuel.TNVED, UpdateSourceTrigger=PropertyChanged}"/>
        </dxlc:LayoutItem>

        <dxlc:LayoutItem Label="НДС">
            <dxe:CheckEdit EditValue="{Binding Fuel.ValueAddedTax}"
                           HorizontalAlignment="Left"/>
        </dxlc:LayoutItem>

        <dxlc:LayoutItem Label="НсП">
            <dxe:SpinEdit MaskUseAsDisplayFormat="True" 
                          Mask="p0"
                          MinValue="0"
                          EditValueType="{x:Type sys:Decimal}"
                          MaxValue="0.05"
                          EditValue="{Binding Fuel.SalesTax, UpdateSourceTrigger=PropertyChanged}" 
                          Increment="0.01"/>
        </dxlc:LayoutItem>

        <dxlc:LayoutGroup HorizontalAlignment="Right"
                          VerticalAlignment="Top">

            <Button Style="{StaticResource SaveBtton}"/>

            <Button Style="{StaticResource CancelButton}"/>

        </dxlc:LayoutGroup>

    </dxlc:LayoutControl>

</local:BaseView>
