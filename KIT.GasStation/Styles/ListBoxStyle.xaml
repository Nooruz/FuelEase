<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:KIT.GasStation.Controls"
    xmlns:converters="clr-namespace:KIT.GasStation.Converters"
    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
    xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking">

    <converters:NozzleMarginConverter x:Key="NozzleMarginConverter" />
    <converters:NozzleStatusToBackgroundConverter x:Key="NozzleStatusToBackgroundConverter" />

    <Style x:Key="NozzleListBox" TargetType="{x:Type ListBox}">

        <Setter Property="Padding" Value="0" />

        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <!--<StackPanel Orientation="Horizontal"/>-->
                    <!--<controls:StretchyWrapPanel VerticalAlignment="Stretch"
                                                    Background="Green"/>-->
                    <!--<UniformGrid Columns="{Binding RelativeSource={RelativeSource AncestorType=ListBox},
                                 Path=Items.Count,
                                 Converter={StaticResource NozzleColumnsConverter}}"/>-->
                    <controls:NozzleUniformGrid MaxColumns="4" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        CornerRadius="3"
                        SnapsToDevicePixels="True">

                        <ScrollViewer
                            Focusable="False"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="Hidden">
                            <ItemsPresenter />
                        </ScrollViewer>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <Style x:Key="NozzleListBoxItem" TargetType="ListBoxItem">
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="ToolTipService.InitialShowDelay" Value="400" />
        <Setter Property="ToolTipService.ShowDuration" Value="8000" />
        <Setter Property="BorderBrush" Value="Silver" />
        <Setter Property="Margin">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource NozzleMarginConverter}">
                    <Binding RelativeSource="{RelativeSource Self}" />
                    <Binding Path="Items.Count" RelativeSource="{RelativeSource AncestorType={x:Type ListBox}}" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="Background">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource NozzleStatusToBackgroundConverter}">
                    <Binding Path="Status" />
                    <Binding Path="IsProgramControl" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border
                        Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="2"
                        CornerRadius="3"
                        SnapsToDevicePixels="True">

                        <controls:NozzleControl />

                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource ControllerSelectedColorBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>


    </Style>

</ResourceDictionary>
