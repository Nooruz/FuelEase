<local:BaseView
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:KIT.GasStation.Views.Base"
    x:Class="KIT.GasStation.Views.RevaluationView"
                    mc:Ignorable="d" 
             Height="450" Width="800">

    <dx:LoadingDecorator>

        <dxlc:LayoutControl Orientation="Vertical">

            <dxg:GridControl ItemsSource="{Binding FuelRevaluations}">
                <dxg:GridControl.View>
                    <dxg:TableView />
                </dxg:GridControl.View>
                <dxg:GridColumn Header="Наименование" 
                        FieldName="Fuel.Name"/>
                <dxg:GridColumn Header="Новая цена" 
                        FieldName="NewPrice">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings MaskType="Numeric" 
                                      Mask="n" 
                                      MaskUseAsDisplayFormat="True"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
                <dxg:GridColumn Header="Старая цена" 
                        ReadOnly="True"
                        FieldName="OldPrice">
                    <dxg:GridColumn.EditSettings>
                        <dxe:TextEditSettings MaskType="Numeric" 
                                      Mask="n" 
                                      MaskUseAsDisplayFormat="True"/>
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
            </dxg:GridControl>

            <dxlc:LayoutGroup HorizontalAlignment="Right"
                      VerticalAlignment="Top">

                <Button Content="Переоценка"
                Command="{Binding StartRevaluationCommand}"/>

                <Button Content="Отмена"
                Command="{Binding CloseCommand}"/>

            </dxlc:LayoutGroup>

        </dxlc:LayoutControl>

    </dx:LoadingDecorator>

</local:BaseView>
