<base:BaseView
    x:Class="KIT.GasStation.Views.Info.CompletionInformationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:KIT.GasStation.Views.Base"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    Width="500">

    <dxlc:LayoutControl Orientation="Vertical">

        <Label
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Content="{Binding CompletedFuelSale.PaymentType.Name}"
            FontSize="24" />

        <dxlc:LayoutItem Label="Сумма клиента">
            <dxe:TextEdit
                FontSize="18"
                Mask="n2"
                MaskType="Numeric"
                MaskUseAsDisplayFormat="True"
                Text="{Binding CompletedFuelSale.Sum}" />
        </dxlc:LayoutItem>

        <dxlc:LayoutItem Label="Сумма продажи">
            <dxe:TextEdit
                FontSize="18"
                Mask="n2"
                MaskType="Numeric"
                MaskUseAsDisplayFormat="True"
                Text="{Binding CompletedFuelSale.ReceivedSum}" />
        </dxlc:LayoutItem>

        <dxlc:LayoutItem Label="Возврат">
            <dxe:TextEdit
                FontSize="18"
                Mask="n2"
                MaskType="Numeric"
                MaskUseAsDisplayFormat="True"
                Text="{Binding Return}" />
        </dxlc:LayoutItem>

        <dxlc:LayoutItem Label="Подробно" LabelVerticalAlignment="Top">
            <dxe:TextEdit
                Height="150"
                VerticalContentAlignment="Top"
                FontSize="18"
                Text="{Binding Info}"
                TextWrapping="Wrap" />
        </dxlc:LayoutItem>

        <dxlc:LayoutGroup HorizontalAlignment="Right" VerticalAlignment="Bottom">

            <Button
                Width="80"
                Command="{Binding CloseCommand}"
                Content="Ок" />

        </dxlc:LayoutGroup>

    </dxlc:LayoutControl>

</base:BaseView>
