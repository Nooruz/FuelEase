<local:BaseView
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    xmlns:local="clr-namespace:FuelEase.Views.Base"
    x:Class="FuelEase.Views.EventPanelView"
    MaxHeight="1600">

    <dxg:GridControl ItemsSource="{Binding Events}" 
                     SelectedItem="{Binding SelectedEventPanel}">

        <dxg:GridControl.View>
            <dxg:TableView ShowGroupPanel="False"
                           AutoWidth="True"
                           AllowEditing="False">
                <dxg:TableView.FormatConditions>
                    <dxg:FormatCondition ApplyToRow="true" Expression="[RowData.Row.Type] = 'Ошибка'" FieldName="RowData.Row.Type">
                        <dx:Format Background="#FFFF9B98"/>
                    </dxg:FormatCondition>
                </dxg:TableView.FormatConditions>
            </dxg:TableView>
        </dxg:GridControl.View>

        <dxg:GridColumn Header="Дата"
                        Width="30"
                        FieldName="CreatedDate">
            <dxg:GridColumn.EditSettings>
                <dxe:DateEditSettings MaskUseAsDisplayFormat="True"
                                      Mask="G"/>
            </dxg:GridColumn.EditSettings>
        </dxg:GridColumn>

        <dxg:GridColumn Header="Сообщения"
                        FieldName="Message"/>

        <dxg:GridColumn Header="Тип"
                        Width="23"
                        FieldName="Type">
            <dxg:GridColumn.EditSettings>
                <dxe:ComboBoxEditSettings ItemsSource="{Binding EventPanelTypes}"
                                          DisplayMember="Value"
                                          ValueMember="Key"/>
            </dxg:GridColumn.EditSettings>
        </dxg:GridColumn>

    </dxg:GridControl>

</local:BaseView>
