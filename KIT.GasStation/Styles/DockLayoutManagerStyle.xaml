<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:KIT.GasStation.Controls"
    xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking">

    <Style x:Key="FuelTransferColumn" TargetType="{x:Type dxdo:DocumentPanel}">
        <Setter Property="Caption" Value="{Binding Caption}" />
        <Setter Property="ShowCloseButton" Value="False" />
        <Setter Property="CaptionHorizontalAlignment" Value="Center" />
        <Setter Property="ShowMaximizeButton" Value="False" />
        <Setter Property="ShowMinimizeButton" Value="False" />
        <Setter Property="AllowSizing" Value="True" />
        <Setter Property="AllowDrag" Value="True" />
        <Setter Property="AllowDrop" Value="False" />
        <Setter Property="AllowMaximize" Value="False" />
        <Setter Property="AllowContextMenu" Value="False" />
        <!--<Setter Property="MinWidth" Value="300"/>-->
        <Setter Property="Padding" Value="0" />
        <Setter Property="MDIMergeStyle" Value="Always" />
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border>

                        <controls:FuelTransferColumnControl />

                    </Border>
                    <!--<DataTemplate.Triggers>
                        <Trigger Property="IsActive" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource TankMinimumForegroundColorBrush}"/>
                        </Trigger>
                    </DataTemplate.Triggers>-->
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Default" TargetType="{x:Type dxdo:DocumentPanel}">
        <Setter Property="Caption" Value="{Binding Caption}" />
        <Setter Property="ShowCloseButton" Value="False" />
        <Setter Property="CaptionHorizontalAlignment" Value="Center" />
        <Setter Property="ShowMaximizeButton" Value="False" />
        <Setter Property="ShowMinimizeButton" Value="False" />
        <Setter Property="AllowSizing" Value="True" />
        <Setter Property="AllowDrag" Value="True" />
        <Setter Property="AllowDrop" Value="False" />
        <Setter Property="AllowMaximize" Value="False" />
        <Setter Property="AllowContextMenu" Value="False" />
        <Setter Property="MinWidth" Value="300" />
        <Setter Property="MinHeight" Value="300" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MDIMergeStyle" Value="Always" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type dxdo:DocumentPanel}">
                    <Border
                        Background="White"
                        BorderBrush="Black"
                        BorderThickness="2" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border Background="Green">

                        <ContentPresenter />

                    </Border>
                    <!--<DataTemplate.Triggers>
                    <Trigger Property="IsActive" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource TankMinimumForegroundColorBrush}"/>
                    </Trigger>
                </DataTemplate.Triggers>-->
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
