<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking" 
                    xmlns:controls="clr-namespace:KIT.GasStation.Controls">

    <Style TargetType="{x:Type dxdo:DocumentPanel}" x:Key="FuelTransferColumn">
        <Setter Property="Caption" Value="{Binding Caption}"/>
        <Setter Property="ShowCloseButton" Value="False"/>
        <Setter Property="CaptionHorizontalAlignment" Value="Center"/>
        <Setter Property="ShowMaximizeButton" Value="False"/>
        <Setter Property="ShowMinimizeButton" Value="False"/>
        <Setter Property="AllowSizing" Value="True"/>
        <Setter Property="AllowDrag" Value="True"/>
        <Setter Property="AllowDrop" Value="False"/>
        <Setter Property="AllowMaximize" Value="False"/>
        <Setter Property="AllowContextMenu" Value="False"/>
        <!--<Setter Property="MinWidth" Value="300"/>-->
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="BorderBrush" Value="{StaticResource TankForegroundColorBrush}"/>
        <Setter Property="Background" Value="#F8F8F8"/>
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border >

                        <controls:FuelTransferColumnControl/>

                    </Border>
                    <!--<DataTemplate.Triggers>
                        <Trigger Property="IsActive" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource TankMinimumForegroundColorBrush}"/>
                        </Trigger>
                    </DataTemplate.Triggers>-->
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>