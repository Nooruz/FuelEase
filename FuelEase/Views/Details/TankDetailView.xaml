<local:BaseView
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:FuelEase.Views.Base"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
    x:Class="FuelEase.Views.Details.TankDetailView"
    Width="350">

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:EventToCommand Event="Loaded"
                               Command="{Binding UserControlLoadedCommand}"/>
    </dxmvvm:Interaction.Behaviors>

    <dxlc:LayoutControl Orientation="Vertical">
        <dxlc:LayoutItem Label="Наименование">
            <dxe:TextEdit EditValue="{Binding Tank.Name, UpdateSourceTrigger=PropertyChanged}"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="Код резервуара">
            <dxe:TextEdit EditValue="{Binding Tank.Number, UpdateSourceTrigger=PropertyChanged}"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="Топливо">
            <dxe:ComboBoxEdit ItemsSource="{Binding Fuels}"
                              DisplayMember="Name"
                              ValueMember="Id"
                              ShowErrorToolTip="False"
                              EditValue="{Binding Tank.FuelId}"
                              ShowError="False"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="Емкость, л">
            <dxe:TextEdit EditValue="{Binding Tank.Size, UpdateSourceTrigger=PropertyChanged}" 
                          MaskUseAsDisplayFormat="True" 
                          MaskType="Numeric" 
                          Mask="n"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutItem Label="Мертвый остаток, л">
            <dxe:TextEdit EditValue="{Binding Tank.MinimumSize, UpdateSourceTrigger=PropertyChanged}" 
                          MaskUseAsDisplayFormat="True" 
                          MaskType="Numeric" 
                          Mask="n"/>
        </dxlc:LayoutItem>
        <dxlc:LayoutGroup HorizontalAlignment="Right"
                          VerticalAlignment="Top">

            <Button Style="{StaticResource SaveBtton}"/>

            <Button Style="{StaticResource CancelButton}"/>

        </dxlc:LayoutGroup>
    </dxlc:LayoutControl>

</local:BaseView>
